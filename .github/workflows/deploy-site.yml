name: Deploy and test site 

on: 
  push:
  pull_request:    
  release:
    types: [published]

jobs:
  build-site:
    name: Build site
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: true
      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: '0.80.0'
          extended: true # required to compile SASS 
      - name: Build Hugo site
        run: hugo --minify 

  deploy-site:
    name: Deploy site
    runs-on: ubuntu-latest
    needs: [build-site]
    outputs:
      deploy-url: ${{ steps.netlify-deploy.outputs.deploy-url }}
    steps:
      - name: Deploy to Netlify
        uses: nwtgck/actions-netlify@v1.2
        id: netlify-deploy
        with:
          publish-dir: './public'
          production-branch: main
          github-token: ${{ secrets.GITHUB_TOKEN }}
          deploy-message: "GitHub Action deploy to production"
          enable-commit-comment: false
          enable-pull-request-comment: true
          alias: ${{ github.head_ref }}
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
        timeout-minutes: 1
  
  check-404s:
    name: Check for 404s
    runs-on: ubuntu-latest
    needs: [build-site]
    steps:
      - name: Start local server to test 
        run: hugo server -D --port 1313 --baseURL http://localhost:1313/
      - name: Check for 404s
        uses: ruzickap/action-my-broken-link-checker@v2
        with:
          # url: ${{ needs.deploy-site.outputs.deploy-url }}
          url: http://localhost:1313/
